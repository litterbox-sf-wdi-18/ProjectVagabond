<% if current_user == @log.user %>

  <%= form_for @log, url: user_log_path(@log.user, @log), method: "put" do |f| %>
    <%= f.label "Title: " %>
    <%= f.text_field :title %><br><br>
    <%= f.label "Content: " %><br>
    <%= f.text_area :content, size: "60x12" %>
    <%= f.submit "Update", class: 'btn btn-primary' %>
  <% end %>

<% else %>

<p>You are not authorized to view this page</p>

<%#=form_for @log, :url => logs_path, :html => { :multipart => true } do |f| %>
  <%#= #f.file_field :image %>
<%# #end %>